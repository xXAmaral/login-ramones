import styled from "styled-components";

export const AreaLogin = styled.div`
    background-color: #fff;

    .form-imput(
        text-aling: left;

        label(
            display: block;
        )
    )
`;